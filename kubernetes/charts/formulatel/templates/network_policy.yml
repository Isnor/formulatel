apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: formulatel-mosquitto-network-guard
  namespace: formulatel
spec:
  # Target the Mosquitto pod deployed by the bjw-s app-template chart
  podSelector:
    matchLabels:
      app.kubernetes.io/name: formulatel-mosquitto
  policyTypes:
    - Ingress
  ingress:
    # Rule A: Allow ONLY your persist service to connect to the anonymous raw MQTT port
    - from:
        - podSelector:
            matchLabels:
              app: persist
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: grafana
      ports:
        - protocol: TCP
          port: 1883

    - ports:
        - protocol: TCP
          port: 9001